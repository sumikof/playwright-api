apiVersion: v1
kind: Service
metadata:
  name: playwright-api
  labels:
    app: playwright-api
spec:
  type: ClusterIP
  selector:
    app: playwright-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
